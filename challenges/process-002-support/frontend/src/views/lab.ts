import type {Mount,Data} from '../types';
import {api,post,escape,number,percent,link,spaceFor,panel,notice,badge,empty,table,bindTicketDetails} from '../ui';
let draft='',last:Data|null=null;
export const mount:Mount=async(root,c)=>{
 const ds=c.params.get('dataset_id')||'ds2';
 root.innerHTML=`<div class="grid-even"><section class="panel"><div class="panel-heading"><h2>Escreva uma solicitação</h2>${badge(ds==='ds2'?'Classificação + busca':'Busca semântica','gold')}</div><p class="small muted">Use um texto livre. A análise executa o pipeline real e preserva as evidências desta versão.</p><form id="lab-form"><div class="field"><label for="ticket-text">Descrição do ticket</label><textarea id="ticket-text" name="text" required minlength="3" maxlength="10000" placeholder="Descreva o problema, o contexto e o que já foi tentado…">${escape(draft)}</textarea><small id="char-count">${draft.length} / 10.000 caracteres</small></div><label class="checkbox"><input type="checkbox" name="add" ${c.params.get('session_id')?'':'disabled'}><span>Adicionar à sessão ativa${c.params.get('session_id')?'':' · inicie uma sessão no Command Center'}</span></label><div class="actions"><button class="button" type="submit">Analisar ticket</button><a class="text-link small" href="${escape(link('command-center',c.params))}">Gerenciar sessão</a></div><p id="lab-status" class="small muted section-gap" role="status"></p><p id="lab-error" class="form-error" role="alert"></p></form><hr class="subtle-divider"><p class="small muted">${ds==='ds2'?'O classificador foi avaliado em tickets IT em inglês. Entradas em PT-BR não possuem benchmark rotulado. Prioridade e rota são sugestões de política.':'Este corpus permite recuperar vizinhos e resoluções históricas. A classificação de oito categorias foi avaliada apenas no suporte de TI e fica indisponível aqui.'}</p></section><section class="panel" id="lab-result"><div class="result-empty">${empty('A decisão vem com evidências','Após a análise, veja categoria, escore, rota sugerida, revisão humana e os vizinhos recuperados.')}</div></section></div><div id="lab-neighbors" class="section-gap"></div>`;
 const form=root.querySelector<HTMLFormElement>('#lab-form')!,textarea=root.querySelector<HTMLTextAreaElement>('#ticket-text')!;textarea.oninput=()=>{draft=textarea.value;root.querySelector('#char-count')!.textContent=`${draft.length} / 10.000 caracteres`;};
 const display=(r:Data)=>{if(c.signal.aborted)return;last=r;const patch={ticket_id:r.ticket_id,session_id:r.session_id,dataset_id:r.dataset_id,space_id:r.space_id,model_version:r.model_version,source:r.source};const duplicate=r.potential_duplicate&&typeof r.potential_duplicate==='object'?r.potential_duplicate:null;const cluster=r.nearest_cluster&&typeof r.nearest_cluster==='object'?r.nearest_cluster:null;root.querySelector('#lab-result')!.innerHTML=`${r.processing_status==='partial'?notice('Processamento parcial',Array.isArray(r.errors)?r.errors.map((e:Data|string)=>typeof e==='string'?e:e.message||JSON.stringify(e)).join(' · '):'Uma etapa está indisponível. Revise os resultados antes de agir.'):""}<div class="decision-box"><div class="badge-row">${badge(r.inference_mode==='online'?'Inferência online':'Artefato pré-calculado')}${badge(r.human_review_required?'Revisão humana necessária':'Triagem elegível',r.human_review_required?'warn':'gold')}</div><h2>${escape(r.predicted_category||'Classificação não aplicável')}</h2><p>${r.confidence==null?'O corpus selecionado não possui classificador validado.':`Escore do classificador: ${percent(r.confidence)}. Similaridade dos vizinhos é uma medida diferente.`}</p></div><dl class="facts"><div><dt>Prioridade sugerida por política</dt><dd>${escape(r.suggested_priority||'Indisponível')}</dd></div><div><dt>Rota sugerida por política</dt><dd>${escape(r.suggested_route||'Revisão humana')}</dd></div><div><dt>Cluster mais próximo</dt><dd>${cluster?`${escape(cluster.cluster_id||'Não associado')} · força ${cluster.strength==null?'indisponível':number(cluster.strength,4)} · ${escape(cluster.method||'método não informado')}`:escape(typeof r.nearest_cluster==='string'?r.nearest_cluster:'Não associado')}</dd></div><div><dt>Latência da inferência</dt><dd>${number(typeof r.latency_ms==='object'?r.latency_ms?.total:r.latency_ms,1)} ms</dd></div><div><dt>Possível duplicata</dt><dd>${duplicate?`${escape(duplicate.ticket_id||'Candidato encontrado')} · similaridade ${number(duplicate.similarity,4)} · limiar ${number(duplicate.threshold,2)} · ${escape(duplicate.policy_version||'política não informada')}`:'Nenhum candidato pela política'}</dd></div><div><dt>Modo de inferência</dt><dd>${escape(r.inference_mode||'Indisponível')}</dd></div><div><dt>Versão do modelo</dt><dd>${escape(r.model_version||'Não aplicável')}</dd></div></dl>${r.review_reasons?.length?`<p class="small muted section-gap"><strong>Motivos de revisão:</strong> ${r.review_reasons.map(escape).join(' · ')}</p>`:''}<div class="actions section-gap"><a class="button" href="${escape(link('graph',c.params,patch))}">Ver na rede semântica</a><a class="button secondary" href="${escape(link('copilot',c.params,patch))}">Abrir no Copilot</a></div><details><summary>Identidade e método</summary><p>Ticket: ${escape(r.ticket_id)}<br>Política: ${escape(r.policy_version||'Indisponível')}<br>Posição: ${escape(r.position_method||'A consultar no Graph')}<br>Status: ${escape(r.processing_status||'Processado')}</p></details>`;const neighbors=r.similar_tickets||[];root.querySelector('#lab-neighbors')!.innerHTML=panel('Por que estes tickets estão próximos?',neighbors.length?table(['Ticket histórico','Categoria','Similaridade','Texto recuperado'],neighbors.map((t:Data)=>[`<button class="text-button" data-ticket="${escape(t.ticket_id)}">${escape(t.ticket_id)}</button>`,escape(t.category||'—'),number(t.similarity,4),`<span class="ticket-snippet">${escape(t.text)}</span>`]),'Similaridade calculada no espaço de embeddings; não é probabilidade de resolução.') :empty('Nenhum vizinho disponível','Não serão criadas conexões sem evidência.'));bindTicketDetails(root,c);};
 if(c.params.get('ticket_id')){const t=await api(`/tickets/${encodeURIComponent(c.params.get('ticket_id')!)}`,{signal:c.signal});if(t.similar_tickets)display(t);}else if(last?.dataset_id===ds)display(last);
 let pending:Data|null=null;form.onsubmit=async e=>{e.preventDefault();const text=textarea.value.trim();if(text.length<3){root.querySelector('#lab-error')!.textContent='Descreva o ticket com pelo menos 3 caracteres.';return;}const button=form.querySelector<HTMLButtonElement>('button')!;button.disabled=true;root.querySelector('#lab-error')!.textContent='';root.querySelector('#lab-status')!.textContent='Executando classificação, embedding e recuperação de vizinhos…';const add=(form.elements.namedItem('add') as HTMLInputElement).checked;if(!pending||pending.text!==text||pending.add_to_live_session!==add)pending={request_id:crypto.randomUUID(),text,dataset_id:ds,space_id:spaceFor(c)?.space_id,session_id:c.params.get('session_id')||undefined,add_to_live_session:add};try{const result=await post('/tickets/analyze',pending);pending=null;display(result);const next=link('ticket-lab',c.params,{ticket_id:result.ticket_id,session_id:result.session_id,dataset_id:result.dataset_id,space_id:result.space_id,model_version:result.model_version,source:result.source});if(location.hash!==next)location.hash=next;root.querySelector('#lab-status')!.textContent='Análise registrada. Evidências disponíveis ao lado.';}catch(error){if(!c.signal.aborted){root.querySelector('#lab-error')!.textContent=(error as Error).message;root.querySelector('#lab-status')!.textContent='O texto foi preservado. Tentar novamente reutiliza a mesma requisição.';}}finally{button.disabled=false;}};
};
